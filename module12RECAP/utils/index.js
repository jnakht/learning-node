
const {addNumbers} = require('./add')
const {subNumbers} = require('./subtract')
const {mulNumbers} = require('./mul')
const {divNumbers} = require('./div')


module.exports = {
    addNumbers,
    subNumbers,
    mulNumbers,
    divNumbers,
}